server:
  port: 9091
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: 123456
    url: jdbc:mysql://localhost:3306/book?userUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=UTC
  jackson:
    time-zone: GMT+8
    date-format: yyyy-MM-dd HH:mm:ss
#mybatis:
#  configuration:
#    map-underscore-to-camel-case: true
#  config-location: classpath:mapper/*.xml
logging:
  pattern:
    console: "[%thread] %-5level %logger{36} - %msg%n"

mybatis_config_file: mybatis-config.xml
mapper_path: /mapper/**.xml
entity_package: com.pg.book.entity